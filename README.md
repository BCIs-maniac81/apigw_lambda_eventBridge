# apigw_lambda_eventBridge
Deploy AWS API Gateway, EventBridge, and Lambda with Terraform

this error must be fixed:
"policy" contains an invalid JSON policy: leading space characters are not allowed
